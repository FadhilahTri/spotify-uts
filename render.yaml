services:
  - type: web
    name: spotify-uts
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir app app:app --workers 1 --threads 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
    plan: free
